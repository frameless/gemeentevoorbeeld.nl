import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Mijn Omgeving Inloggen',
  description: '...',
};

const Page = () => {
  return (
    <div>
      <h1>Mijn Omgeving</h1>
    </div>
  );
};

export default Page;
